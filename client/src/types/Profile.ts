interface Profile {
  displayName: string;
  phoneNumber: string;
  photoURL: string;
  email: string;
  role: string;
  isActive: boolean;
  isActivated: boolean;
  address: string;
  affiliate: string;
  token: string;
}

export default Profile;