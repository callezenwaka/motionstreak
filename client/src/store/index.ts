import { createStore } from 'vuex'
import account from './modules/account';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    account
  }
})
